<%= form_with(model: tickets_user, local: true) do |form| %>
  <% if tickets_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tickets_user.errors.count, "error") %> prohibited this tickets_user from being saved:</h2>

      <ul>
      <% tickets_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<nav class="navbar navbar-inverse">
  <div class="container">
    <div class="navbar-header">
      <%= link_to 'Gym Beam', root_path, class: 'navbar-brand' %>
    </div>
    <div id="navbar">
  
    <ul class="nav navbar-nav pull-right">
      <% if user_signed_in? %>
        <% if current_user.admin? %>
          <li><%= link_to 'Unapproved users', users_unapproved_path %></li> 
        <% end %>
      <% end %>
      <li><%= link_to 'My tickets', tickets_users_path %></li>
      <li><%= link_to 'Tickets', tickets_path %></li>
      <li><%= link_to 'Sport sections', sport_sections_path %></li>
      <ul class="nav navbar-nav pull-right">
        <% if user_signed_in? %>
          <li><%= current_user.email %></li>
          <% if current_user.admin? %>
            <li>(ADMIN)</li> 
          <% end %>
          <li><%= link_to 'Log out', destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to 'Log In', new_user_session_path %></li>
          <li><%= link_to 'Sign Up', new_user_registration_path %></li>
        <% end %>
      </ul>
    </ul>
  </div>
</nav>